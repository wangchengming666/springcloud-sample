server:
    port: 8762

spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                git:
                    uri:
                    searchPaths:
                    username:
                    password:
            label: master

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/